version: '3.8'

# Plain HTTP (OK for localhost only). For a public VPS use:
#   deployment/vps/docker-compose.caddy.yml
services:
  addon:
    build: .
    container_name: tg-stremio-addon
    ports:
      - "7860:7860"
    env_file:
      - .env
    restart: unless-stopped
